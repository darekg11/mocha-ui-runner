# WIP

Run like so:
1. In first terminal `npm run watch-web-app` to build and hot-reload UI
2. In second terminal `npm run dev -- --server_port=5000 --server_log_level=debug --mocha_tests_path='test-examples/**/*.spec.js'` to start server and UI on port `5000` with `DEBUG` log level and tests to run from relative `test-examples/**/*.spec.js` path
