const SERVER_DEFAULTS = {
  PORT: 11234,
  LOG_LEVEL: "info",
};

const MOCHA_DEFAULTS = {
  TESTS_PATH: "",
  UI: "bdd",
};

const RUNNER_STATUS = {
  NOT_STARTED: "NOT_STARTED",
  IN_PROGRESS: "IN_PROGRESS",
  FINISHED: "FINISHED",
};

module.exports = {
  SERVER_DEFAULTS,
  MOCHA_DEFAULTS,
  RUNNER_STATUS,
};
